<project name="clean-stage">

	<target name="Clean">

		<delete dir="${artifacts.dir}" if="${directory::exists(artifacts.dir)}" />
		<mkdir dir="${artifacts.dir}" />
		<mkdir dir="${artifacts.dir}\NUnit" />

	</target>

</project>